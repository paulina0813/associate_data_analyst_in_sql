# Databases

- A table is a component of a database
    - They organize data into rows and columns
        - Rows contain individual data
        - Columns describe a specific part of the data
- Several tables usually make up a database, and they work together through relationships 
- Relational databases include tables that share information, creating connections between them
